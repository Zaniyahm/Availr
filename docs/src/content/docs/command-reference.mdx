# Command Reference

Here’s a quick breakdown of the npm commands used in this project.

| Command           | Function                                                                |
|-------------------|-------------------------------------------------------------------------|
| `npm install`     | Installs all project dependencies listed in `package.json`.             |
| `npm run dev`     | Starts a local development server with live reloading.                  |
| `npm run build`   | Builds a production-ready version of the site.                          |
| `npm run preview` | Previews the static site locally after building.                        |

These commands are typically run inside the `docs/` folder.

